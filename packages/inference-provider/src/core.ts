import type { InitializableProvider } from "@tryopenbot/runtime-provider";

/** Inference credentials and external account provisioning, never an authored-agent model API. */
export interface InferenceProvider extends InitializableProvider {}
