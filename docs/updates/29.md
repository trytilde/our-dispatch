# Intent of the change

Bring the maintained Tilde fork onto the latest canonical `trytilde/dispatch` main while preserving fork-owned agents, configuration, deployment history, and resources.

# Architecture changes

The fork remains a public GitHub fork. Its `upstream` remote now targets `trytilde/dispatch`, and its origin is `trytilde/our-dispatch`. The merge adds typed ChatKit execution context consumption and current canonical repository/bootstrap URLs without replacing fork-owned configuration.

# Summarized package changes

- Merges the latest canonical Dispatch runtime and generated Tilde API client.
- Updates canonical and fork repository references after the GitHub rename.
- Preserves existing fork commits and deployment configuration.

# Critical to apply to forks

yes — pull this merge before the next fork deployment so its runtime consumes the API's typed durable execution contract.
