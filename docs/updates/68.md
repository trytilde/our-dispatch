# Intent of the change

Give Computer sessions a coherent GTK XFCE desktop with provider-owned, deterministic defaults.

# Architecture changes

No provider boundary changes. The computer-service provider remains the sole owner of desktop image assets.

```mermaid
flowchart LR
  P[Computer provider assets] --> I[Computer image]
  I --> V[VNC clients]
```

# Summarized package changes

- `@tryopenbot/computer-service-provider`: XFCE session, panel, launcher, theme, and wallpaper assets.

# Critical to apply to forks

Yes. Forks that build their own Computer image must rebuild it after taking this change.

<FOLLOW UP>
Owner: apps/mobile
Trigger: when Expo gains an owner Computer surface
Work: render the shared preview URL natively and preserve explicit full-screen takeover semantics
</FOLLOW UP>

