# Intent of the change

Reduce Tilde reconciliation latency without unbounded fan-out or nondeterministic result ordering.

# Architecture changes

No contract changes. A bounded internal helper remains inside the Tilde agent-provider adapter.

```mermaid
flowchart LR
  D[Desired resources] --> B[Bounded mapper]
  B --> T[Tilde API]
  T --> O[Ordered results]
```

# Summarized package changes

- `@tryopenbot/agent-provider`: bounded skill and tool reconciliation.
- `@tryopenbot/computer-service`: scheduler-independent concurrency test assertion.

# Critical to apply to forks

No. The lifecycle remains idempotent and configuration-compatible.

