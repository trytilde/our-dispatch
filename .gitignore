/plan/
node_modules/
.turbo/
.cache/
dist/
coverage/
playwright-report/
storybook-static/
test-results/
.data/
/.env
/.env.*
# Fork-owned: configuration/.env (public identifiers, resource IDs, endpoints) and the
# SOPS-encrypted secrets document are tracked so every checkout has them. Only the
# decrypted rendering stays local.
configuration/secrets.yaml
.vercel/
.openbot-deploy/
.openbot/
*.log
apps/web/src/routeTree.gen.ts
apps/desktop/out/
apps/desktop/release/
.expo/
apps/mobile/android/
apps/mobile/ios/
.vercel
.claude/worktrees/
